name: test
description: used for testing
inputs:
  repo:
    description: repository
    required: true
  revision:
    description: revision of repository
    default: master
    required: false
  username:
    description: user to clone the repo
    required: true
  token:
    description: token to clone the repo
    required: true
run:
  main: python3 ./werun/khorium.py
